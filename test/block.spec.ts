/*import Block from '../static/src/vendor/block/index';
var jsdom = require('mocha-jsdom')
import {assert} from 'chai';


describe('Работоспособность компоненты', () => {
    beforeEach(function () {
        jsdom();
    });
    function createBlock() : Block {
        return new Block('div')
    }
    it('Компонента должна уметь скрывать элемент', () => {

        const block : Block = createBlock();

        block.hide();

        assert.equal(block.element.style.display, 'none');


    })

    it('Компонента должна уметь показывать элемент', () => {

        const block : Block = createBlock();

        block.show();

        assert.equal(block.element.style.display, 'block');
    })

    it('Компонента должна уметь рендерить элемент', () => {

        const block : Block = createBlock();

        block._render();

        assert.equal(block.element.innerHTML, '123');
    })

})*/
